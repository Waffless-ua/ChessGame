<UserControl x:Class="ChessGame.View.UserControls.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChessGame.View.UserControls"
             mc:Ignorable="d"
             Width="720" Height="360">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="1" Style="{StaticResource ButtonStyle}" Margin="0,10" Command="{Binding CreateGameCommand}">Створити гру</Button>
            <Button Grid.Row="2" Style="{StaticResource ButtonStyle}" Margin="0,10" Command="{Binding SearchGameCommand}">Знайти гру</Button>
            <Button Grid.Row="3" Style="{StaticResource ButtonStyle}" Margin="0,10" Command="{Binding SettingsCommand}">Налаштування</Button>
            <Button Grid.Row="4" Style="{StaticResource ButtonStyle}" Margin="0,10" Command="{Binding ExitCommand}">Вийти</Button>
        </Grid>
    </Grid>
</UserControl>
