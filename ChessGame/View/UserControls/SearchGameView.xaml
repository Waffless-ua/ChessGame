<UserControl x:Class="ChessGame.View.UserControls.SearchGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChessGame.View.UserControls"
             Width="720" Height="360">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <TextBox Grid.Row="1"
                 Style="{StaticResource TextBoxStyle}"
                 Height="40"
                 MaxLength="15"
                 ToolTip="Введіть IP"
                 Text="{Binding IpAddress}"/>
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Style="{StaticResource ButtonStyle}" 
                    Margin="0,10" Command="{Binding JoinCommand}">Приєднатися
                </Button>
                <Button Grid.Row="1" Style="{StaticResource ButtonStyle}" 
                    Margin="0,10" Command="{Binding MenuCommand}">Меню
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
